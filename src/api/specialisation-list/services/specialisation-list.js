'use strict';

/**
 * specialisation-list service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::specialisation-list.specialisation-list');
